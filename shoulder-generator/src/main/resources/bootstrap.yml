spring:
  #allow-bean-definition-overriding: true
  application:
    name: generator
  cloud:
    nacos:
      config:
        server-addr: itlym.cn:8848
        file-extension: yml
        shared-configs:
          - dataId: common.yml
            refresh: true
          - dataId: db.yml
            refresh: true
        enabled: true